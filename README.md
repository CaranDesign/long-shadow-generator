# Long Shadow Generator

A small, open-source **React + TypeScript** project to create customizable long text shadows with live preview, color selection, and code export. Inspired by the “long shadow” style used in graphic design and UI typography.  

---

## 🚀 Features

- **Dynamic text shadow generator** with real-time preview  
- Fully **customizable parameters**:
  - Shadow length (depth)
  - Angle (rotation)
  - Blur
  - Intensity
  - Color  
- **Live text editing**: update the text content directly  
- **Custom font size** with slider control  
- **Copy-ready code**: generates CSS `text-shadow` string  
- **React + TypeScript** for full type safety  
- Clean **modular components** for easy reuse  
- Styled with **Tailwind CSS** and optionally shadcn UI components  

---


## 💻 Installation

1. Clone the repository:

```bash
git clone https://github.com/CaranDesign/long-shadow.git
cd long-shadow
